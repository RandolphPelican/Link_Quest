{
  "name": "link-quest",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build:client": "tsc -p client && vite build client",
    "build:server": "tsc -p server",
    "build": "npm run build:client && npm run build:server",
    "start": "node server.js"
  },
  "dependencies": {
    "phaser": "^3.60.0",
    "colyseus.js": "^0.15.0",
    "colyseus": "^0.15.0",
    "@colyseus/schema": "^2.0.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^4.4.5",
    "ts-node-dev": "^2.0.0",
    "@types/node": "^20.0.0",
    "@types/express": "^4.17.17"
  }
}